# Round r9 — `/v8` changelog

## Summary

| Field | Value |
|-------|-------|
| **Route** | `/v8` |
| **Paper page** | — (reference: DoorDash store Reviews module) |
| **Inherits from** | `/v7` via `V8_CONFIG` spreading `V7_CONFIG` |
| **Stakeholder goal** | Compare a horizontal reviews carousel (rating gauge + AI highlights + review cards) vs the stacked v7 reviews module |
| **Status** | Active |
| **Prod URL** | https://pdp-next-sigma.vercel.app/v8 |

---

## What changed (stakeholder-facing)

- Same PDP as `/v7` (gallery, buy bar, swatch rail, mid-page reviews, desktop split).
- **Land review teaser** becomes a DoorDash-style horizontal carousel:
  - Header with ratings count, **Add review**, and photo shortcut
  - Scrollable cards: circular star gauge → AI review highlights → individual review snippets
- Mid-page Reviews module stays as on `/v7`.

---

## What changed (implementation)

| Area | Flags / files | Notes |
|------|---------------|-------|
| Route | `src/app/v8/` | `layout.tsx`, pages, `pdp-v8.css`, root marker |
| Config | `V8_CONFIG` | Spreads `V7_CONFIG`; `useCarouselReviews: true` |
| Component | `pdp-v8-reviews.tsx` | Carousel land teaser |
| Wiring | `pdp-v5-review-teaser.tsx` | Flag-gated; mid-page unchanged |

---

## Verification

- [ ] `/v8` shows horizontal reviews carousel
- [ ] `/v7` keeps prior stacked reviews
- [ ] `/v1`–`/v6` unchanged
- [ ] `pnpm check:versions` passes

---

## Compare URLs

Local:  http://localhost:3000/v7   ← stacked reviews  
        http://localhost:3000/v8   ← carousel reviews  

Prod:   https://pdp-next-sigma.vercel.app/v7  
        https://pdp-next-sigma.vercel.app/v8  

Swap `v7` ↔ `v8` in the path to A/B on the same deploy.

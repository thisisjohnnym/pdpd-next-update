# Round r7 — `/v6` changelog

## Summary

| Field | Value |
|-------|-------|
| **Route** | `/v6` |
| **Paper page** | — (screenshot brief) |
| **Inherits from** | `/v5` via `V6_CONFIG` spreading `V5_CONFIG` |
| **Stakeholder goal** | Compare full material-grouped color swatch rail vs v5’s compact “+ Colors” tray chip |
| **Status** | Frozen |
| **Prod URL** | https://pdp-next-sigma.vercel.app/v6 |

---

## What changed (stakeholder-facing)

- Same PDP as `/v5` (gallery, buy bar, modules, desktop split).
- Under the product name / “in Quilted Leather” / price, color options appear as a full horizontal swatch rail.
- Swatches are grouped by material: current material colors first, then a divider + next material label (e.g. Soft Leather) and its colors.

---

## What changed (implementation)

| Area | Flags / files | Notes |
|------|---------------|-------|
| Route | `src/app/v6/` | `layout.tsx`, pages, `pdp-v6.css`, root marker |
| Config | `V6_CONFIG` | Spreads `V5_CONFIG`; sets `heroColorTrayOverlay: false` so the rail renders |
| Rail order | `pdp-buy-bar-compact-color.tsx` | Current material leads the inline rail |

---

## Local / prod links

```
Local:  http://localhost:3000/v5   ← frozen comparison
        http://localhost:3000/v6   ← material-grouped swatch rail

Prod:   https://pdp-next-sigma.vercel.app/v5
        https://pdp-next-sigma.vercel.app/v6
```

Swap `v5` ↔ `v6` in the path to A/B the two options on the same deploy.

# FC01 — final-candidate UXR study pair (Jul 2026)

Brand approved the Skelly template, so this round ships as the **FC (final candidate) pair** for the UXR study — two links, one deploy:

| Route | Variant | Config |
|-------|---------|--------|
| `/fc01` | Horizontal hero gallery (progress bar + category rail) | `FC01_CONFIG` spreads `V5_CONFIG` |
| `/fc01v` | Vertical hero gallery (snap stack + tick indicator, fill-frame stills) | `FC01V_CONFIG` spreads `FC01_CONFIG` + v6 gallery overrides |

## What landed

- **Skelly sync** — merged `skelly/main` (`5beb774`, Find your Tabby carousel family explorer + store pickup / review teaser) into `pdp-fc-1.0`. Prior sync: `74fc299` docked color rail footer + richer aging highlight tray. Color preview variant API remains `"dot" | "swatch" | "rail"`.
- **Find your Tabby** — closer-look stage moved above More like this as a swipeable silhouette carousel (inline size chips + Shop CTA). Mid-gallery “Get up close” slot is off (`showUpCloseModule: false`).
- **FC route pair** — `src/app/fc01/` and `src/app/fc01v/` mirror the v5/v6 route pattern (layout scope, root marker, thin CSS overlay, home + `products/[slug]` pages).
- **Shared CSS via dual scopes** — `pdp-v5.css` and `pdp-v6.css` now scope under `:is([data-pdp-version="v5"], [data-pdp-version="fc01"])` / `:is([data-pdp-version="v6"], [data-pdp-version="fc01v"])`; FC-only tweaks go in `pdp-fc01.css` / `pdp-fc01v.css`.
- **Fall-in intro on FC routes** — the boot script (`pdp-v6-intro-boot-script.ts`) stamps `data-pdp-version` + `data-hero-intro-phase` for `/fc01` and `/fc01v` alongside `/v5`–`/v6`.
- **Boundary script** — CSS scope, provider, and Tabby-URL guards extended to `fc01` / `fc01v`.
- **Compare tray column pattern** — the More like this compare sheet matches Paper `N1T-0`: This bag / Compared to labels, per-column Add to bag, and tighter Details-pattern fact rows.

## Share links (production)

```
fc01 (horizontal): https://pdp-next-sigma.vercel.app/fc01
fc01v (vertical):  https://pdp-next-sigma.vercel.app/fc01v
```

## Notes

- The FC pair is **active work** — tweaks for the UXR study land here (flags in `FC01_CONFIG` / `FC01V_CONFIG`).
- The Skelly sync touches shared components (buy bar row land CTA, compact color dots, highlight detail sheet), so `/v5` and `/v6` also render the updated docked color rail — they are no longer pixel-frozen to their original ship.

## Deploy log

- _Not yet deployed to production._

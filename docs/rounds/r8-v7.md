# Round r8 — `/v7` changelog

## Summary

| Field | Value |
|-------|-------|
| **Route** | `/v7` |
| **Paper page** | — |
| **Inherits from** | `/v6` via `V7_CONFIG` spreading `V6_CONFIG` |
| **Stakeholder goal** | Compare a horizontal ~7-swatch scroll rail vs v6’s See more colorways collapse |
| **Status** | Frozen |
| **Prod URL** | https://pdp-next-sigma.vercel.app/v7 |

---

## What changed (stakeholder-facing)

- Same PDP as `/v6` (gallery, buy bar, modules, desktop split).
- Colorways sit in one horizontal scroll row (~7 visible left-to-right) — no “See more colorways”.

---

## What changed (implementation)

| Area | Flags / files | Notes |
|------|---------------|-------|
| Route | `src/app/v7/` | `layout.tsx`, pages, `pdp-v7.css`, root marker |
| Config | `V7_CONFIG` | Spreads `V6_CONFIG`; `materialSwatchSeeMore: false` → horizontal rail |
| Flag | `materialSwatchSeeMore` | v6 `true` (wrap + See more); v7 `false` (horizontal scroll) |

---

## Verification

- [ ] `/v7` shows a single horizontal swatch rail (no See more)
- [ ] `/v6` still collapses with See more colorways
- [ ] `/v1`–`/v5` unchanged
- [ ] `pnpm check:versions` passes

---

## Compare URLs

Local:  http://localhost:3000/v6   ← See more colorways  
        http://localhost:3000/v7   ← horizontal ~7-swatch rail  

Prod:   https://pdp-next-sigma.vercel.app/v6  
        https://pdp-next-sigma.vercel.app/v7  

Swap `v6` ↔ `v7` in the path to A/B on the same deploy.
